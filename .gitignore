# Backup directory (created by install/sync scripts)
.backup/

# Session-specific data (NEVER commit)
projects/
session-env/
history.jsonl
shell-snapshots/
debug/
todos/
tasks/
plans/
cache/
telemetry/
file-history/
stats-cache.json

# Plugin installations (local plugin cache, not the repo's plugins/ directory)
installed_plugins.json

# Machine-specific files (never tracked — each machine maintains its own)
CLAUDE.md
settings.json
mcp.json
settings.local.json
.env
*.local.md

# Python artifacts
__pycache__/
*.pyc
*.pyo

# Packaged skill archives (binary)
*.skill

# System files
.DS_Store
*.swp
*.log
*.bak
